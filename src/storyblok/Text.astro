---
import { storyblokEditable, renderRichText } from '@storyblok/astro';
import type { SbBlokData } from '@storyblok/astro';
import { blockClasses } from '../lib/blok-style';

interface Props { blok: SbBlokData }
const { blok } = Astro.props;
const html = blok.body ? renderRichText(blok.body as any) : '';
---
<section {...storyblokEditable(blok)} class={blockClasses(blok, 'block--text')}>
  <div class="wrap">
    <div class="aside-grid">
      <div>
        {blok.eyebrow && <p class="eyebrow">{blok.eyebrow}</p>}
        {blok.heading && <h2>{blok.heading}</h2>}
      </div>
      {html && <div class="prose" set:html={html} />}
    </div>
  </div>
</section>
